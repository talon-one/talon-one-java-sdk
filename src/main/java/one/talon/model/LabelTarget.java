/*
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package one.talon.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import one.talon.model.AudienceReference;
import one.talon.model.LabelTargetAudience;
import one.talon.model.LabelTargetNone;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import one.talon.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
public class LabelTarget extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(LabelTarget.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!LabelTarget.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'LabelTarget' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<LabelTargetNone> adapterLabelTargetNone = gson.getDelegateAdapter(this, TypeToken.get(LabelTargetNone.class));
            final TypeAdapter<LabelTargetAudience> adapterLabelTargetAudience = gson.getDelegateAdapter(this, TypeToken.get(LabelTargetAudience.class));

            return (TypeAdapter<T>) new TypeAdapter<LabelTarget>() {
                @Override
                public void write(JsonWriter out, LabelTarget value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `LabelTargetNone`
                    if (value.getActualInstance() instanceof LabelTargetNone) {
                        JsonElement element = adapterLabelTargetNone.toJsonTree((LabelTargetNone)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `LabelTargetAudience`
                    if (value.getActualInstance() instanceof LabelTargetAudience) {
                        JsonElement element = adapterLabelTargetAudience.toJsonTree((LabelTargetAudience)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: LabelTargetAudience, LabelTargetNone");
                }

                @Override
                public LabelTarget read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize LabelTargetNone
                    try {
                        // validate the JSON object to see if any exception is thrown
                        LabelTargetNone.validateJsonElement(jsonElement);
                        actualAdapter = adapterLabelTargetNone;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'LabelTargetNone'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for LabelTargetNone failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'LabelTargetNone'", e);
                    }
                    // deserialize LabelTargetAudience
                    try {
                        // validate the JSON object to see if any exception is thrown
                        LabelTargetAudience.validateJsonElement(jsonElement);
                        actualAdapter = adapterLabelTargetAudience;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'LabelTargetAudience'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for LabelTargetAudience failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'LabelTargetAudience'", e);
                    }

                    if (match == 1) {
                        LabelTarget ret = new LabelTarget();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for LabelTarget: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public LabelTarget() {
        super("oneOf", Boolean.FALSE);
    }

    public LabelTarget(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("LabelTargetNone", LabelTargetNone.class);
        schemas.put("LabelTargetAudience", LabelTargetAudience.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return LabelTarget.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * LabelTargetAudience, LabelTargetNone
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof LabelTargetNone) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof LabelTargetAudience) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be LabelTargetAudience, LabelTargetNone");
    }

    /**
     * Get the actual instance, which can be the following:
     * LabelTargetAudience, LabelTargetNone
     *
     * @return The actual instance (LabelTargetAudience, LabelTargetNone)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `LabelTargetNone`. If the actual instance is not `LabelTargetNone`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `LabelTargetNone`
     * @throws ClassCastException if the instance is not `LabelTargetNone`
     */
    @SuppressWarnings("unchecked")
    public LabelTargetNone getLabelTargetNone() throws ClassCastException {
        return (LabelTargetNone)super.getActualInstance();
    }

    /**
     * Get the actual instance of `LabelTargetAudience`. If the actual instance is not `LabelTargetAudience`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `LabelTargetAudience`
     * @throws ClassCastException if the instance is not `LabelTargetAudience`
     */
    @SuppressWarnings("unchecked")
    public LabelTargetAudience getLabelTargetAudience() throws ClassCastException {
        return (LabelTargetAudience)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to LabelTarget
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with LabelTargetNone
        try {
            LabelTargetNone.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for LabelTargetNone failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with LabelTargetAudience
        try {
            LabelTargetAudience.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for LabelTargetAudience failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for LabelTarget with oneOf schemas: LabelTargetAudience, LabelTargetNone. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of LabelTarget given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of LabelTarget
     * @throws IOException if the JSON string is invalid with respect to LabelTarget
     */
    public static LabelTarget fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, LabelTarget.class);
    }

    /**
     * Convert an instance of LabelTarget to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

