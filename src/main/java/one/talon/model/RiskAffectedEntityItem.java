/*
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package one.talon.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonNull;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import one.talon.JSON;

/**
 * A single entity flagged as anomalous within a risk.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.25.0")
public class RiskAffectedEntityItem {
  public static final String SERIALIZED_NAME_ENTITY_ID = "entityId";
  @SerializedName(SERIALIZED_NAME_ENTITY_ID)
  @javax.annotation.Nonnull
  private String entityId;

  public static final String SERIALIZED_NAME_ACTIVITY_VALUE = "activityValue";
  @SerializedName(SERIALIZED_NAME_ACTIVITY_VALUE)
  @javax.annotation.Nonnull
  private Double activityValue;

  public static final String SERIALIZED_NAME_THRESHOLD = "threshold";
  @SerializedName(SERIALIZED_NAME_THRESHOLD)
  @javax.annotation.Nonnull
  private Double threshold;

  public static final String SERIALIZED_NAME_SEVERITY_RATIO = "severityRatio";
  @SerializedName(SERIALIZED_NAME_SEVERITY_RATIO)
  @javax.annotation.Nonnull
  private Double severityRatio;

  /**
   * The critical classification bucket of this entity.
   */
  @JsonAdapter(CriticalityEnum.Adapter.class)
  public enum CriticalityEnum {
    CRITICAL("critical"),
    
    NOT_CRITICAL("not_critical");

    private String value;

    CriticalityEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static CriticalityEnum fromValue(String value) {
      for (CriticalityEnum b : CriticalityEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<CriticalityEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final CriticalityEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public CriticalityEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return CriticalityEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      CriticalityEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_CRITICALITY = "criticality";
  @SerializedName(SERIALIZED_NAME_CRITICALITY)
  @javax.annotation.Nonnull
  private CriticalityEnum criticality;

  public RiskAffectedEntityItem() {
  }

  public RiskAffectedEntityItem entityId(@javax.annotation.Nonnull String entityId) {
    this.entityId = entityId;
    return this;
  }

  /**
   * The integration ID of the affected entity.
   * @return entityId
   */
  @javax.annotation.Nonnull
  public String getEntityId() {
    return entityId;
  }

  public void setEntityId(@javax.annotation.Nonnull String entityId) {
    this.entityId = entityId;
  }


  public RiskAffectedEntityItem activityValue(@javax.annotation.Nonnull Double activityValue) {
    this.activityValue = activityValue;
    return this;
  }

  /**
   * The observed value of the monitored activity metric for this entity.
   * @return activityValue
   */
  @javax.annotation.Nonnull
  public Double getActivityValue() {
    return activityValue;
  }

  public void setActivityValue(@javax.annotation.Nonnull Double activityValue) {
    this.activityValue = activityValue;
  }


  public RiskAffectedEntityItem threshold(@javax.annotation.Nonnull Double threshold) {
    this.threshold = threshold;
    return this;
  }

  /**
   * The anomaly threshold computed for the entity&#39;s Application group.
   * @return threshold
   */
  @javax.annotation.Nonnull
  public Double getThreshold() {
    return threshold;
  }

  public void setThreshold(@javax.annotation.Nonnull Double threshold) {
    this.threshold = threshold;
  }


  public RiskAffectedEntityItem severityRatio(@javax.annotation.Nonnull Double severityRatio) {
    this.severityRatio = severityRatio;
    return this;
  }

  /**
   * The ratio of the observed value to the threshold.
   * @return severityRatio
   */
  @javax.annotation.Nonnull
  public Double getSeverityRatio() {
    return severityRatio;
  }

  public void setSeverityRatio(@javax.annotation.Nonnull Double severityRatio) {
    this.severityRatio = severityRatio;
  }


  public RiskAffectedEntityItem criticality(@javax.annotation.Nonnull CriticalityEnum criticality) {
    this.criticality = criticality;
    return this;
  }

  /**
   * The critical classification bucket of this entity.
   * @return criticality
   */
  @javax.annotation.Nonnull
  public CriticalityEnum getCriticality() {
    return criticality;
  }

  public void setCriticality(@javax.annotation.Nonnull CriticalityEnum criticality) {
    this.criticality = criticality;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the RiskAffectedEntityItem instance itself
   */
  public RiskAffectedEntityItem putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    RiskAffectedEntityItem riskAffectedEntityItem = (RiskAffectedEntityItem) o;
    return Objects.equals(this.entityId, riskAffectedEntityItem.entityId) &&
        Objects.equals(this.activityValue, riskAffectedEntityItem.activityValue) &&
        Objects.equals(this.threshold, riskAffectedEntityItem.threshold) &&
        Objects.equals(this.severityRatio, riskAffectedEntityItem.severityRatio) &&
        Objects.equals(this.criticality, riskAffectedEntityItem.criticality)&&
        Objects.equals(this.additionalProperties, riskAffectedEntityItem.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(entityId, activityValue, threshold, severityRatio, criticality, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class RiskAffectedEntityItem {\n");
    sb.append("    entityId: ").append(toIndentedString(entityId)).append("\n");
    sb.append("    activityValue: ").append(toIndentedString(activityValue)).append("\n");
    sb.append("    threshold: ").append(toIndentedString(threshold)).append("\n");
    sb.append("    severityRatio: ").append(toIndentedString(severityRatio)).append("\n");
    sb.append("    criticality: ").append(toIndentedString(criticality)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("entityId", "activityValue", "threshold", "severityRatio", "criticality"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("entityId", "activityValue", "threshold", "severityRatio", "criticality"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to RiskAffectedEntityItem
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!RiskAffectedEntityItem.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in RiskAffectedEntityItem is not found in the empty JSON string", RiskAffectedEntityItem.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : RiskAffectedEntityItem.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("entityId").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `entityId` to be a primitive type in the JSON string but got `%s`", jsonObj.get("entityId").toString()));
      }
      if (!jsonObj.get("criticality").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `criticality` to be a primitive type in the JSON string but got `%s`", jsonObj.get("criticality").toString()));
      }
      // validate the required field `criticality`
      CriticalityEnum.validateJsonElement(jsonObj.get("criticality"));
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!RiskAffectedEntityItem.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'RiskAffectedEntityItem' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<RiskAffectedEntityItem> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(RiskAffectedEntityItem.class));

       return (TypeAdapter<T>) new TypeAdapter<RiskAffectedEntityItem>() {
           @Override
           public void write(JsonWriter out, RiskAffectedEntityItem value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonNull()) {
                     obj.add(entry.getKey(), JsonNull.INSTANCE);
                   } else if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public RiskAffectedEntityItem read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             RiskAffectedEntityItem instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of RiskAffectedEntityItem given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of RiskAffectedEntityItem
   * @throws IOException if the JSON string is invalid with respect to RiskAffectedEntityItem
   */
  public static RiskAffectedEntityItem fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, RiskAffectedEntityItem.class);
  }

  /**
   * Convert an instance of RiskAffectedEntityItem to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

