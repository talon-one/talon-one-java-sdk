/*
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package one.talon.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import one.talon.model.CheckAttributeBlock;
import one.talon.model.PassthroughBlock;
import one.talon.model.SelectorGroupBlock;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import one.talon.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
public class SelectorBlock extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SelectorBlock.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SelectorBlock.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SelectorBlock' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CheckAttributeBlock> adapterCheckAttributeBlock = gson.getDelegateAdapter(this, TypeToken.get(CheckAttributeBlock.class));
            final TypeAdapter<SelectorGroupBlock> adapterSelectorGroupBlock = gson.getDelegateAdapter(this, TypeToken.get(SelectorGroupBlock.class));
            final TypeAdapter<PassthroughBlock> adapterPassthroughBlock = gson.getDelegateAdapter(this, TypeToken.get(PassthroughBlock.class));

            return (TypeAdapter<T>) new TypeAdapter<SelectorBlock>() {
                @Override
                public void write(JsonWriter out, SelectorBlock value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `CheckAttributeBlock`
                    if (value.getActualInstance() instanceof CheckAttributeBlock) {
                        JsonElement element = adapterCheckAttributeBlock.toJsonTree((CheckAttributeBlock)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `SelectorGroupBlock`
                    if (value.getActualInstance() instanceof SelectorGroupBlock) {
                        JsonElement element = adapterSelectorGroupBlock.toJsonTree((SelectorGroupBlock)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `PassthroughBlock`
                    if (value.getActualInstance() instanceof PassthroughBlock) {
                        JsonElement element = adapterPassthroughBlock.toJsonTree((PassthroughBlock)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock");
                }

                @Override
                public SelectorBlock read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize CheckAttributeBlock
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CheckAttributeBlock.validateJsonElement(jsonElement);
                        actualAdapter = adapterCheckAttributeBlock;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'CheckAttributeBlock'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for CheckAttributeBlock failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CheckAttributeBlock'", e);
                    }
                    // deserialize SelectorGroupBlock
                    try {
                        // validate the JSON object to see if any exception is thrown
                        SelectorGroupBlock.validateJsonElement(jsonElement);
                        actualAdapter = adapterSelectorGroupBlock;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'SelectorGroupBlock'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for SelectorGroupBlock failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'SelectorGroupBlock'", e);
                    }
                    // deserialize PassthroughBlock
                    try {
                        // validate the JSON object to see if any exception is thrown
                        PassthroughBlock.validateJsonElement(jsonElement);
                        actualAdapter = adapterPassthroughBlock;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'PassthroughBlock'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for PassthroughBlock failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'PassthroughBlock'", e);
                    }

                    if (match == 1) {
                        SelectorBlock ret = new SelectorBlock();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for SelectorBlock: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public SelectorBlock() {
        super("oneOf", Boolean.FALSE);
    }

    public SelectorBlock(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("CheckAttributeBlock", CheckAttributeBlock.class);
        schemas.put("SelectorGroupBlock", SelectorGroupBlock.class);
        schemas.put("PassthroughBlock", PassthroughBlock.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return SelectorBlock.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof CheckAttributeBlock) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof SelectorGroupBlock) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof PassthroughBlock) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock");
    }

    /**
     * Get the actual instance, which can be the following:
     * CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock
     *
     * @return The actual instance (CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `CheckAttributeBlock`. If the actual instance is not `CheckAttributeBlock`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CheckAttributeBlock`
     * @throws ClassCastException if the instance is not `CheckAttributeBlock`
     */
    @SuppressWarnings("unchecked")
    public CheckAttributeBlock getCheckAttributeBlock() throws ClassCastException {
        return (CheckAttributeBlock)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SelectorGroupBlock`. If the actual instance is not `SelectorGroupBlock`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SelectorGroupBlock`
     * @throws ClassCastException if the instance is not `SelectorGroupBlock`
     */
    @SuppressWarnings("unchecked")
    public SelectorGroupBlock getSelectorGroupBlock() throws ClassCastException {
        return (SelectorGroupBlock)super.getActualInstance();
    }

    /**
     * Get the actual instance of `PassthroughBlock`. If the actual instance is not `PassthroughBlock`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `PassthroughBlock`
     * @throws ClassCastException if the instance is not `PassthroughBlock`
     */
    @SuppressWarnings("unchecked")
    public PassthroughBlock getPassthroughBlock() throws ClassCastException {
        return (PassthroughBlock)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SelectorBlock
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with CheckAttributeBlock
        try {
            CheckAttributeBlock.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for CheckAttributeBlock failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with SelectorGroupBlock
        try {
            SelectorGroupBlock.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for SelectorGroupBlock failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with PassthroughBlock
        try {
            PassthroughBlock.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for PassthroughBlock failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for SelectorBlock with oneOf schemas: CheckAttributeBlock, PassthroughBlock, SelectorGroupBlock. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of SelectorBlock given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SelectorBlock
     * @throws IOException if the JSON string is invalid with respect to SelectorBlock
     */
    public static SelectorBlock fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SelectorBlock.class);
    }

    /**
     * Convert an instance of SelectorBlock to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

