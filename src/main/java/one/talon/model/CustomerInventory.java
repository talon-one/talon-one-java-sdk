/*
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) are used to integrate with our platform - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment. For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}` 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package one.talon.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import one.talon.model.AchievementProgressWithDefinition;
import one.talon.model.CustomerProfile;
import one.talon.model.Giveaway;
import one.talon.model.InventoryCoupon;
import one.talon.model.InventoryReferral;
import one.talon.model.Loyalty;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import one.talon.JSON;

/**
 * CustomerInventory
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.19.0")
public class CustomerInventory {
  public static final String SERIALIZED_NAME_PROFILE = "profile";
  @SerializedName(SERIALIZED_NAME_PROFILE)
  @javax.annotation.Nullable
  private CustomerProfile profile;

  public static final String SERIALIZED_NAME_LOYALTY = "loyalty";
  @SerializedName(SERIALIZED_NAME_LOYALTY)
  @javax.annotation.Nullable
  private Loyalty loyalty;

  public static final String SERIALIZED_NAME_REFERRALS = "referrals";
  @SerializedName(SERIALIZED_NAME_REFERRALS)
  @javax.annotation.Nullable
  private List<InventoryReferral> referrals = new ArrayList<>();

  public static final String SERIALIZED_NAME_COUPONS = "coupons";
  @SerializedName(SERIALIZED_NAME_COUPONS)
  @javax.annotation.Nullable
  private List<InventoryCoupon> coupons = new ArrayList<>();

  public static final String SERIALIZED_NAME_GIVEAWAYS = "giveaways";
  @SerializedName(SERIALIZED_NAME_GIVEAWAYS)
  @javax.annotation.Nullable
  private List<Giveaway> giveaways = new ArrayList<>();

  public static final String SERIALIZED_NAME_ACHIEVEMENTS = "achievements";
  @SerializedName(SERIALIZED_NAME_ACHIEVEMENTS)
  @javax.annotation.Nullable
  private List<AchievementProgressWithDefinition> achievements = new ArrayList<>();

  public CustomerInventory() {
  }

  public CustomerInventory profile(@javax.annotation.Nullable CustomerProfile profile) {
    this.profile = profile;
    return this;
  }

  /**
   * Get profile
   * @return profile
   */
  @javax.annotation.Nullable
  public CustomerProfile getProfile() {
    return profile;
  }

  public void setProfile(@javax.annotation.Nullable CustomerProfile profile) {
    this.profile = profile;
  }


  public CustomerInventory loyalty(@javax.annotation.Nullable Loyalty loyalty) {
    this.loyalty = loyalty;
    return this;
  }

  /**
   * Get loyalty
   * @return loyalty
   */
  @javax.annotation.Nullable
  public Loyalty getLoyalty() {
    return loyalty;
  }

  public void setLoyalty(@javax.annotation.Nullable Loyalty loyalty) {
    this.loyalty = loyalty;
  }


  public CustomerInventory referrals(@javax.annotation.Nullable List<InventoryReferral> referrals) {
    this.referrals = referrals;
    return this;
  }

  public CustomerInventory addReferralsItem(InventoryReferral referralsItem) {
    if (this.referrals == null) {
      this.referrals = new ArrayList<>();
    }
    this.referrals.add(referralsItem);
    return this;
  }

  /**
   * Get referrals
   * @return referrals
   */
  @javax.annotation.Nullable
  public List<InventoryReferral> getReferrals() {
    return referrals;
  }

  public void setReferrals(@javax.annotation.Nullable List<InventoryReferral> referrals) {
    this.referrals = referrals;
  }


  public CustomerInventory coupons(@javax.annotation.Nullable List<InventoryCoupon> coupons) {
    this.coupons = coupons;
    return this;
  }

  public CustomerInventory addCouponsItem(InventoryCoupon couponsItem) {
    if (this.coupons == null) {
      this.coupons = new ArrayList<>();
    }
    this.coupons.add(couponsItem);
    return this;
  }

  /**
   * The coupons reserved by this profile. This array includes hard and soft reservations. 
   * @return coupons
   */
  @javax.annotation.Nullable
  public List<InventoryCoupon> getCoupons() {
    return coupons;
  }

  public void setCoupons(@javax.annotation.Nullable List<InventoryCoupon> coupons) {
    this.coupons = coupons;
  }


  public CustomerInventory giveaways(@javax.annotation.Nullable List<Giveaway> giveaways) {
    this.giveaways = giveaways;
    return this;
  }

  public CustomerInventory addGiveawaysItem(Giveaway giveawaysItem) {
    if (this.giveaways == null) {
      this.giveaways = new ArrayList<>();
    }
    this.giveaways.add(giveawaysItem);
    return this;
  }

  /**
   * Get giveaways
   * @return giveaways
   */
  @javax.annotation.Nullable
  public List<Giveaway> getGiveaways() {
    return giveaways;
  }

  public void setGiveaways(@javax.annotation.Nullable List<Giveaway> giveaways) {
    this.giveaways = giveaways;
  }


  public CustomerInventory achievements(@javax.annotation.Nullable List<AchievementProgressWithDefinition> achievements) {
    this.achievements = achievements;
    return this;
  }

  public CustomerInventory addAchievementsItem(AchievementProgressWithDefinition achievementsItem) {
    if (this.achievements == null) {
      this.achievements = new ArrayList<>();
    }
    this.achievements.add(achievementsItem);
    return this;
  }

  /**
   * Get achievements
   * @return achievements
   */
  @javax.annotation.Nullable
  public List<AchievementProgressWithDefinition> getAchievements() {
    return achievements;
  }

  public void setAchievements(@javax.annotation.Nullable List<AchievementProgressWithDefinition> achievements) {
    this.achievements = achievements;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CustomerInventory customerInventory = (CustomerInventory) o;
    return Objects.equals(this.profile, customerInventory.profile) &&
        Objects.equals(this.loyalty, customerInventory.loyalty) &&
        Objects.equals(this.referrals, customerInventory.referrals) &&
        Objects.equals(this.coupons, customerInventory.coupons) &&
        Objects.equals(this.giveaways, customerInventory.giveaways) &&
        Objects.equals(this.achievements, customerInventory.achievements);
  }

  @Override
  public int hashCode() {
    return Objects.hash(profile, loyalty, referrals, coupons, giveaways, achievements);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CustomerInventory {\n");
    sb.append("    profile: ").append(toIndentedString(profile)).append("\n");
    sb.append("    loyalty: ").append(toIndentedString(loyalty)).append("\n");
    sb.append("    referrals: ").append(toIndentedString(referrals)).append("\n");
    sb.append("    coupons: ").append(toIndentedString(coupons)).append("\n");
    sb.append("    giveaways: ").append(toIndentedString(giveaways)).append("\n");
    sb.append("    achievements: ").append(toIndentedString(achievements)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("profile", "loyalty", "referrals", "coupons", "giveaways", "achievements"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CustomerInventory
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CustomerInventory.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in CustomerInventory is not found in the empty JSON string", CustomerInventory.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!CustomerInventory.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `CustomerInventory` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `profile`
      if (jsonObj.get("profile") != null && !jsonObj.get("profile").isJsonNull()) {
        CustomerProfile.validateJsonElement(jsonObj.get("profile"));
      }
      // validate the optional field `loyalty`
      if (jsonObj.get("loyalty") != null && !jsonObj.get("loyalty").isJsonNull()) {
        Loyalty.validateJsonElement(jsonObj.get("loyalty"));
      }
      if (jsonObj.get("referrals") != null && !jsonObj.get("referrals").isJsonNull()) {
        JsonArray jsonArrayreferrals = jsonObj.getAsJsonArray("referrals");
        if (jsonArrayreferrals != null) {
          // ensure the json data is an array
          if (!jsonObj.get("referrals").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `referrals` to be an array in the JSON string but got `%s`", jsonObj.get("referrals").toString()));
          }

          // validate the optional field `referrals` (array)
          for (int i = 0; i < jsonArrayreferrals.size(); i++) {
            InventoryReferral.validateJsonElement(jsonArrayreferrals.get(i));
          };
        }
      }
      if (jsonObj.get("coupons") != null && !jsonObj.get("coupons").isJsonNull()) {
        JsonArray jsonArraycoupons = jsonObj.getAsJsonArray("coupons");
        if (jsonArraycoupons != null) {
          // ensure the json data is an array
          if (!jsonObj.get("coupons").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `coupons` to be an array in the JSON string but got `%s`", jsonObj.get("coupons").toString()));
          }

          // validate the optional field `coupons` (array)
          for (int i = 0; i < jsonArraycoupons.size(); i++) {
            InventoryCoupon.validateJsonElement(jsonArraycoupons.get(i));
          };
        }
      }
      if (jsonObj.get("giveaways") != null && !jsonObj.get("giveaways").isJsonNull()) {
        JsonArray jsonArraygiveaways = jsonObj.getAsJsonArray("giveaways");
        if (jsonArraygiveaways != null) {
          // ensure the json data is an array
          if (!jsonObj.get("giveaways").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `giveaways` to be an array in the JSON string but got `%s`", jsonObj.get("giveaways").toString()));
          }

          // validate the optional field `giveaways` (array)
          for (int i = 0; i < jsonArraygiveaways.size(); i++) {
            Giveaway.validateJsonElement(jsonArraygiveaways.get(i));
          };
        }
      }
      if (jsonObj.get("achievements") != null && !jsonObj.get("achievements").isJsonNull()) {
        JsonArray jsonArrayachievements = jsonObj.getAsJsonArray("achievements");
        if (jsonArrayachievements != null) {
          // ensure the json data is an array
          if (!jsonObj.get("achievements").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `achievements` to be an array in the JSON string but got `%s`", jsonObj.get("achievements").toString()));
          }

          // validate the optional field `achievements` (array)
          for (int i = 0; i < jsonArrayachievements.size(); i++) {
            AchievementProgressWithDefinition.validateJsonElement(jsonArrayachievements.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CustomerInventory.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CustomerInventory' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CustomerInventory> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CustomerInventory.class));

       return (TypeAdapter<T>) new TypeAdapter<CustomerInventory>() {
           @Override
           public void write(JsonWriter out, CustomerInventory value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CustomerInventory read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CustomerInventory given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CustomerInventory
   * @throws IOException if the JSON string is invalid with respect to CustomerInventory
   */
  public static CustomerInventory fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CustomerInventory.class);
  }

  /**
   * Convert an instance of CustomerInventory to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

