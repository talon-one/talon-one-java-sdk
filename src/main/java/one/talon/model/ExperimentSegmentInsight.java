/*
 * Talon.One API
 * Use the Talon.One API to integrate with your application and to manage applications and campaigns:  - Use the operations in the [Integration API section](#integration-api) to integrate with our platform. - Use the operation in the [Management API section](#management-api) to manage applications and campaigns.  ## Determining the base URL of the endpoints  The API is available at the same hostname as your Campaign Manager deployment.  For example, if you access the Campaign Manager at `https://yourbaseurl.talon.one/`, the URL for the [updateCustomerSessionV2](https://docs.talon.one/integration-api#tag/Customer-sessions/operation/updateCustomerSessionV2) endpoint is `https://yourbaseurl.talon.one/v2/customer_sessions/{Id}`. 
 *
 * The version of the OpenAPI document: 
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package one.talon.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import one.talon.model.ExperimentSegmentInsightVariant;
import com.google.gson.JsonElement;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import one.talon.JSON;

/**
 * ExperimentSegmentInsight
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.21.0")
public class ExperimentSegmentInsight {
  /**
   * The segmentation dimension used to group customers or purchases for analysis.
   */
  @JsonAdapter(DimensionEnum.Adapter.class)
  public enum DimensionEnum {
    CART_VALUE("cart_value"),
    
    ITEM_COUNT("item_count"),
    
    CUSTOMER_TYPE("customer_type");

    private String value;

    DimensionEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static DimensionEnum fromValue(String value) {
      for (DimensionEnum b : DimensionEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<DimensionEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final DimensionEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public DimensionEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return DimensionEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      DimensionEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_DIMENSION = "dimension";
  @SerializedName(SERIALIZED_NAME_DIMENSION)
  @javax.annotation.Nonnull
  private DimensionEnum dimension;

  /**
   * The specific group within the segmentation dimension.
   */
  @JsonAdapter(BucketEnum.Adapter.class)
  public enum BucketEnum {
    LOW("low"),
    
    MEDIUM("medium"),
    
    HIGH("high"),
    
    NEW("new"),
    
    RETURNING("returning"),
    
    LOYAL("loyal");

    private String value;

    BucketEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static BucketEnum fromValue(String value) {
      for (BucketEnum b : BucketEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<BucketEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final BucketEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public BucketEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return BucketEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      BucketEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_BUCKET = "bucket";
  @SerializedName(SERIALIZED_NAME_BUCKET)
  @javax.annotation.Nonnull
  private BucketEnum bucket;

  public static final String SERIALIZED_NAME_CONFIDENCE = "confidence";
  @SerializedName(SERIALIZED_NAME_CONFIDENCE)
  @javax.annotation.Nonnull
  private Double confidence;

  public static final String SERIALIZED_NAME_WINNER_VARIANT_ID = "winnerVariantId";
  @SerializedName(SERIALIZED_NAME_WINNER_VARIANT_ID)
  @javax.annotation.Nonnull
  private Long winnerVariantId;

  public static final String SERIALIZED_NAME_VARIANTS = "variants";
  @SerializedName(SERIALIZED_NAME_VARIANTS)
  @javax.annotation.Nonnull
  private List<ExperimentSegmentInsightVariant> variants = new ArrayList<>();

  public ExperimentSegmentInsight() {
  }

  public ExperimentSegmentInsight dimension(@javax.annotation.Nonnull DimensionEnum dimension) {
    this.dimension = dimension;
    return this;
  }

  /**
   * The segmentation dimension used to group customers or purchases for analysis.
   * @return dimension
   */
  @javax.annotation.Nonnull
  public DimensionEnum getDimension() {
    return dimension;
  }

  public void setDimension(@javax.annotation.Nonnull DimensionEnum dimension) {
    this.dimension = dimension;
  }


  public ExperimentSegmentInsight bucket(@javax.annotation.Nonnull BucketEnum bucket) {
    this.bucket = bucket;
    return this;
  }

  /**
   * The specific group within the segmentation dimension.
   * @return bucket
   */
  @javax.annotation.Nonnull
  public BucketEnum getBucket() {
    return bucket;
  }

  public void setBucket(@javax.annotation.Nonnull BucketEnum bucket) {
    this.bucket = bucket;
  }


  public ExperimentSegmentInsight confidence(@javax.annotation.Nonnull Double confidence) {
    this.confidence = confidence;
    return this;
  }

  /**
   * The raw (unadjusted) confidence score expressed as a percentage. Only segments with a confidence score greater than or equal to 95% are returned. 
   * minimum: 95
   * maximum: 100
   * @return confidence
   */
  @javax.annotation.Nonnull
  public Double getConfidence() {
    return confidence;
  }

  public void setConfidence(@javax.annotation.Nonnull Double confidence) {
    this.confidence = confidence;
  }


  public ExperimentSegmentInsight winnerVariantId(@javax.annotation.Nonnull Long winnerVariantId) {
    this.winnerVariantId = winnerVariantId;
    return this;
  }

  /**
   * The ID of the variant that performed better in this segment.
   * @return winnerVariantId
   */
  @javax.annotation.Nonnull
  public Long getWinnerVariantId() {
    return winnerVariantId;
  }

  public void setWinnerVariantId(@javax.annotation.Nonnull Long winnerVariantId) {
    this.winnerVariantId = winnerVariantId;
  }


  public ExperimentSegmentInsight variants(@javax.annotation.Nonnull List<ExperimentSegmentInsightVariant> variants) {
    this.variants = variants;
    return this;
  }

  public ExperimentSegmentInsight addVariantsItem(ExperimentSegmentInsightVariant variantsItem) {
    if (this.variants == null) {
      this.variants = new ArrayList<>();
    }
    this.variants.add(variantsItem);
    return this;
  }

  /**
   * Per-variant metric values for this segment.
   * @return variants
   */
  @javax.annotation.Nonnull
  public List<ExperimentSegmentInsightVariant> getVariants() {
    return variants;
  }

  public void setVariants(@javax.annotation.Nonnull List<ExperimentSegmentInsightVariant> variants) {
    this.variants = variants;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the ExperimentSegmentInsight instance itself
   */
  public ExperimentSegmentInsight putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ExperimentSegmentInsight experimentSegmentInsight = (ExperimentSegmentInsight) o;
    return Objects.equals(this.dimension, experimentSegmentInsight.dimension) &&
        Objects.equals(this.bucket, experimentSegmentInsight.bucket) &&
        Objects.equals(this.confidence, experimentSegmentInsight.confidence) &&
        Objects.equals(this.winnerVariantId, experimentSegmentInsight.winnerVariantId) &&
        Objects.equals(this.variants, experimentSegmentInsight.variants)&&
        Objects.equals(this.additionalProperties, experimentSegmentInsight.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(dimension, bucket, confidence, winnerVariantId, variants, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ExperimentSegmentInsight {\n");
    sb.append("    dimension: ").append(toIndentedString(dimension)).append("\n");
    sb.append("    bucket: ").append(toIndentedString(bucket)).append("\n");
    sb.append("    confidence: ").append(toIndentedString(confidence)).append("\n");
    sb.append("    winnerVariantId: ").append(toIndentedString(winnerVariantId)).append("\n");
    sb.append("    variants: ").append(toIndentedString(variants)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("dimension", "bucket", "confidence", "winnerVariantId", "variants"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("dimension", "bucket", "confidence", "winnerVariantId", "variants"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ExperimentSegmentInsight
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ExperimentSegmentInsight.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ExperimentSegmentInsight is not found in the empty JSON string", ExperimentSegmentInsight.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : ExperimentSegmentInsight.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("dimension").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `dimension` to be a primitive type in the JSON string but got `%s`", jsonObj.get("dimension").toString()));
      }
      // validate the required field `dimension`
      DimensionEnum.validateJsonElement(jsonObj.get("dimension"));
      if (!jsonObj.get("bucket").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `bucket` to be a primitive type in the JSON string but got `%s`", jsonObj.get("bucket").toString()));
      }
      // validate the required field `bucket`
      BucketEnum.validateJsonElement(jsonObj.get("bucket"));
      if (jsonObj.get("variants") != null) {
        if (!jsonObj.get("variants").isJsonArray()) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `variants` to be an array in the JSON string but got `%s`", jsonObj.get("variants").toString()));
        }
        JsonArray jsonArrayvariants = jsonObj.getAsJsonArray("variants");
        // validate the required field `variants` (array)
        for (int i = 0; i < jsonArrayvariants.size(); i++) {
          ExperimentSegmentInsightVariant.validateJsonElement(jsonArrayvariants.get(i));
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ExperimentSegmentInsight.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ExperimentSegmentInsight' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ExperimentSegmentInsight> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ExperimentSegmentInsight.class));

       return (TypeAdapter<T>) new TypeAdapter<ExperimentSegmentInsight>() {
           @Override
           public void write(JsonWriter out, ExperimentSegmentInsight value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public ExperimentSegmentInsight read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             ExperimentSegmentInsight instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ExperimentSegmentInsight given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ExperimentSegmentInsight
   * @throws IOException if the JSON string is invalid with respect to ExperimentSegmentInsight
   */
  public static ExperimentSegmentInsight fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ExperimentSegmentInsight.class);
  }

  /**
   * Convert an instance of ExperimentSegmentInsight to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

